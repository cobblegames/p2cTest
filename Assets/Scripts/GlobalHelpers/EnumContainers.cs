public enum PlayerAction
{
    Pickup = 0,
    Drop = 1,
    Use = 2,
}


public enum PlayerAlarmStatus 
{ NotDetected = 0, 
    Detected = 1 
}

public enum ObjectStatus
{
    Static = 0,
    Moving = 1,
    Stolen = 2
}